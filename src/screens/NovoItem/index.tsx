import React from "react";
import { View, Text } from "react-native";


const NovoItem = () => {
    return(
        <View style={{flexDirection:"column", alignItems:"center", justifyContent:"center", flex:1}}>
            <Text style={{fontSize:20}}>NOVO ITEM</Text>
        </View>
    )
}

export default NovoItem